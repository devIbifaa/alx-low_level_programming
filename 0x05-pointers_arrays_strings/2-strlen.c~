#include <stdio.h>

/**
 *_strlen(char *s) - main fuction to print the length of s
 *@s: the string
 *Return: length
 */
int _strlen(char *s)
{
int length = 0;
while (*s != '\0')
{
length++;
s++;
}
 return (length);
}
